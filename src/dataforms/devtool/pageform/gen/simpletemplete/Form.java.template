package ${packageName};

import dataforms.controller.Form;

/**
 * ${pageName} 用フォームクラス。
 */
public class ${formClassName} extends Form {
	/**
	 * コンストラクタ。
	 */
	public ${formClassName}() {
		super(null);
		// TODO:フィールを追加します。
	}

	@Override
	public void init() throws Exception {
		super.init();
	}
}
